Final Gmail direct contact fix:
- The Send Request / Envoyer la demande buttons now open Gmail compose directly.
- The form details are inserted automatically into the email body.
- No backend service, no FormSubmit activation, no setup needed.

Git:
git add .
git commit -m "Make contact forms open Gmail directly"
git push -f origin main
